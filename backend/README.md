# LLM Wiki Service

?????? LLM Wiki ????,?? FastAPI ???????????? AI ???????????,?????????

## ????

```
llm-wiki-service/
??? app/
?   ??? main.py                    # FastAPI ????(?Git?????)
?   ??? config.py                  # ????(??????,?? .env)
?   ??? models/                    # ????
?   ?   ??? document.py            # ????
?   ?   ??? wiki.py                # Wiki??
?   ??? services/                  # ??????
?   ?   ??? document_service.py    # ????
?   ?   ??? wiki_service.py        # Wiki????
?   ?   ??? ai_service.py          # AI??(????LLM???)
?   ?   ??? search_service.py      # ??(???+????)
?   ?   ??? pipeline_service.py    # ???????
?   ?   ??? git_sync_service.py    # Git????
?   ??? tools/                     # AI??(???LangChain)
?   ?   ??? file_tools.py          # ??????
?   ?   ??? code_tools.py          # ????
?   ??? api/                       # REST API??
?       ??? documents.py
?       ??? wiki.py
?       ??? ai.py
?       ??? search.py
?       ??? pipeline.py
??? data/                          # ??????(????)
?   ??? raw/                       # ?????(AI??)
?   ??? wiki/                      # Wiki???(AI??)
?   ?   ??? pages/                 # Wiki??(Markdown + frontmatter)
?   ?   ??? index/                 # ????
?   ??? vectors/                   # ??????
??? .env.example                   # ??????(??????)
??? requirements.txt
??? README.md
```

## ????

```bash
pip install -r requirements.txt
cp .env.example .env
# ?? .env,?? API Key ???
uvicorn app.main:app --reload
# ?? http://localhost:8000/docs ??API??
```

## ????

??????? `.env` ???,`config.py` ??????????

### ??????

```env
# ===== LLM ??? =====
LLM_PROVIDER=openai                          # openai | anthropic
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_CHAT_MODEL=claude-sonnet-4-20250514

# ===== ???? =====
ENABLE_VECTOR_SEARCH=true                    # true | false

# ===== ???? =====
QA_RETRIEVAL_MODE=ai                         # ai | auto
QA_AI_DIRECT_THRESHOLD=200                   # auto????(Wiki???)

# ===== Git ?? =====
GIT_SYNC_ENABLED=false                       # true | false
GIT_SYNC_INTERVAL_MINUTES=30
GIT_REPO_PATH=./data
GIT_REMOTE_URL=
GIT_USER_NAME=LLM Wiki Bot
GIT_USER_EMAIL=llm-wiki-bot@example.com
GIT_BRANCH=main
GIT_COMMIT_MESSAGE=                          # ??????

# ===== ?? =====
DATA_DIR=./data
RAW_DIR=./data/raw
WIKI_DIR=./data/wiki

# ===== ?? =====
APP_NAME=LLM Wiki Service
DEBUG=true
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
```

### ?????

| ??? | ??? | ?? |
|-------|--------|------|
| `LLM_PROVIDER` | `openai` | ?? LLM ???,`openai` ? `anthropic` |
| `ENABLE_VECTOR_SEARCH` | `true` | ??????????,???????? |
| `QA_RETRIEVAL_MODE` | `ai` | ??????,`ai` ???AI????,`auto` ???????? |
| `QA_AI_DIRECT_THRESHOLD` | `200` | `auto` ???,Wiki????????????AI |
| `GIT_SYNC_ENABLED` | `false` | ?????? Git ???? |
| `GIT_SYNC_INTERVAL_MINUTES` | `30` | Git ????(??) |

---

## ??????:????,????

### ??????(??????)

#### 1. ??????

**???:** ????(Markdown/???)

```bash
# ??A:??????(JSON Body)
POST /api/documents
Content-Type: application/json

{
  "filename": "rag-article.md",
  "content": "# RAG????\nRAG?...(??????)",
  "title": "RAG????",
  "tags": ["RAG", "LLM"],
  "category": "AI??"
}

# ??B:????(multipart,?????????)
POST /api/documents/upload
Content-Type: multipart/form-data

file=@rag-article.md
```

**????:** ????? JSON

```json
{
  "id": "a1b2c3d4e5f6",
  "filename": "rag-article.md",
  "content": "# RAG????\nRAG?...",
  "metadata": {
    "title": "rag-article.md",
    "tags": [],
    "category": null,
    "doc_type": "other",
    "created_at": "2026-05-12T15:30:00",
    "file_size": 2048
  }
}
```

#### 2. ?AI????(????)

**???:** ????(???????ID)

```bash
POST /api/pipeline/run
Content-Type: application/json

# ???? = ????????????
{}

# ????????
{
  "doc_ids": ["a1b2c3d4e5f6", "g7h8i9j0k1l2"],
  "auto_rebuild": true
}
```

**????:** ????(AI????)

```json
{
  "started_at": "2026-05-12T15:30:00",
  "finished_at": "2026-05-12T15:32:30",
  "total_documents": 2,
  "summary": { "new_wiki": 2, "merged": 0, "skipped": 0, "error": 0 },
  "results": [
    {
      "doc_id": "a1b2c3d4e5f6",
      "status": "new_wiki",
      "wiki_page_id": "x1y2z3",
      "wiki_page_ids": ["x1y2z3"],
      "merge_type": "new",
      "changes": ["??Wiki??: RAG????", "??? 1 ?Wiki??(???: 1)"]
    }
  ]
}
```

> **???"??? ? ????"??????** ????????,????? pipeline,AI ???????? Wiki?????? Wiki??????

### ????(????????)

**???:** ????????

```bash
POST /api/ai/ask
Content-Type: application/json

{
  "query": "RAG????????"
}
```

**????:** AI ????? + ????

```json
{
  "answer": "RAG?????????:\n1. ????(??:[RAG????])\n2. ????...",
  "sources": [
    {
      "id": "x1y2z3",
      "title": "RAG????",
      "type": "wiki",
      "slug": "rag-ji-shu-yuan-li",
      "relevance_reason": "?????RAG?????"
    }
  ],
  "retrieval": "ai",
  "search_query": "RAG????????"
}
```

### ????Wiki(??????)

**???:** ?? + Markdown??

```bash
# ??Wiki??(JSON Body)
POST /api/wiki/pages
Content-Type: application/json

{
  "title": "Prompt????",
  "content": "# Prompt??\n## ????\n...",
  "tags": ["prompt", "AI??"],
  "category": "AI??",
  "status": "published"
}

# ????Wiki??
GET /api/wiki/pages

# ??????(?????????)
GET /api/wiki/graph
```

**????:** Wiki?? JSON / ???? JSON

### ??????

| ?? | ??? | ???? |
|------|--------|---------|
| ???? | ??ID??? | `{"summary": "??????..."}` |
| ???? | ??ID??? | `{"doc_type": "paper", "tags": ["RAG", "LLM"], "category": "AI??"}` |
| ???? | ??ID??? | `{"entities": [{"name": "RAG", "type": "technology"}]}` |
| ?? | ??? | ?????/Wiki?? |
| ?????? | ??ID | AI???????(???) |
| ?????? | ? | ???????????????? |

### ????????

| ??????? | ????????? |
|--------------|-----------------|
| **??**(Markdown????????) | **???Wiki**(AI????????) |
| **??????** | **??????**(???????,??????) |
| **??**(.md / .txt) | **?????**(ID?????????) |
| **??**("??"?"??"?"??") | **AI????**(JSON????????) |

---

## ??????????

?????? `POST /api/pipeline/run` ?????,AI ??????????????,????????:

### ???:???????

```
??: ????"RAG????"???
???: ??(????Wiki)

AI??:
  topic_count: 1
  topics: [{ topic: "RAG????", action: "create_new" }]

??: 1??Wiki??
????:
  POST /api/pipeline/run
    ? find_unprocessed_documents()
    ? _analyze_new_document()          # AI??:???,??
    ? _create_new_wiki()               # topic_count=1,????
      ? ai_service.generate_wiki_page()  # AI?????Wiki??
      ? wiki_service.create_page()       # ?? data/wiki/pages/
      ? _mark_document_processed()       # ???????
    ? search_service.rebuild_index()    # ??????
```

### ???:???????

```
??: ????????"Prompt??"?"????"?"Agent??"
???: ??(????Wiki)

AI??:
  topic_count: 3
  topics: [
    { topic: "Prompt??", action: "create_new" },
    { topic: "????",   action: "create_new" },
    { topic: "Agent??",  action: "create_new" }
  ]

??: 3??Wiki?? + ??????
????:
  POST /api/pipeline/run
    ? _analyze_new_document()          # AI??:3?????
    ? _create_new_wiki()               # topic_count>1,???????
      ? _ai_split_by_topics()           # AI?????????
      ? ????segment:
          ? ai_service.generate_wiki_page()  # ????Wiki
          ? wiki_service.create_page()
      ? wiki_service.add_related_page()     # ????????
    ? _mark_document_processed()
    ? search_service.rebuild_index()
```

### ???:??????????Wiki

```
??: ????"RAG????"???
???: ??Wiki?? "RAG????"

AI??:
  topic_count: 1
  topics: [{ topic: "RAG????", action: "merge_into", target: "abc123" }]

??: 0??Wiki,??Wiki?v1???v2
????:
  POST /api/pipeline/run
    ? _analyze_new_document()          # AI??:???Wiki??
    ? _merge_into_existing()
      ? wiki_service.get_page("abc123")  # ????Wiki??
      ? _ai_merge_content()               # AI????????
      ? wiki_service.update_page()        # ???? v1?v2
      ? _mark_document_processed()
```

### ???:????(???? + ????)

```
??: ????"??????"???,??RAG?Prompt??
???: ??Wiki?? "RAG????",???Prompt????Wiki

AI??:
  topic_count: 2
  topics: [
    { topic: "RAG??",   action: "merge_into", target: "abc123" },
    { topic: "Prompt??", action: "create_new" }
  ]

??: 1??Wiki + ??1???Wiki
????:
  POST /api/pipeline/run
    ? _analyze_new_document()          # AI??:????
    ? _split_and_merge()               # has_create=true, has_merge=true
      ? _ai_split_document()             # AI????
      ? ?merge?: _ai_merge_content() + update_page()
      ? ?create?: generate_wiki_page() + create_page()
    ? _mark_document_processed()
    ? _ai_rebuild_knowledge_structure()  # ??????
      ? ????????
      ? ????????
```

### ???:??????(??)

```
??: ?????Wiki?????????
???: ??Wiki?? "RAG????"(???????)

AI??:
  topic_count: 1
  topics: [{ topic: "RAG??", action: "skip" }]

??: 0??Wiki,????????
????:
  POST /api/pipeline/run
    ? _analyze_new_document()          # AI??:?????
    ? _mark_document_processed("skipped")  # ????
```

### ????

| ?? | ??? | ?? | merge_type | ???? |
|------|--------|------|-----------|---------|
| ? | 1 | 1??Wiki | `new` | ???,????????? |
| ? | N | N??Wiki + ?? | `new_multi` | ???,?????? |
| ? | 1 | ??1???Wiki | `update` | ???,???Wiki?? |
| ? | N | M?? + K??? | `split` | ???,???????? |
| ? | 1 | 0(??) | - | ???????? |

---

## API ??

### ????
- `POST /api/documents` - ????
- `POST /api/documents/upload` - ????
- `GET /api/documents` - ????
- `GET /api/documents/{id}` - ????
- `PUT /api/documents/{id}` - ????
- `DELETE /api/documents/{id}` - ????

### Wiki??
- `POST /api/wiki/pages` - ??Wiki??
- `GET /api/wiki/pages` - ??Wiki??
- `GET /api/wiki/pages/{id}` - ??Wiki??
- `PUT /api/wiki/pages/{id}` - ??Wiki??
- `DELETE /api/wiki/pages/{id}` - ??Wiki??
- `GET /api/wiki/graph` - ????????

### AI??
- `POST /api/ai/ask` - ????(??????,??????+??)
- `POST /api/ai/summarize` - ??????
- `POST /api/ai/classify` - ????
- `POST /api/ai/generate-wiki` - ?????Wiki
- `POST /api/ai/chat` - ?????(???????ID)
- `POST /api/ai/extract-entities` - ????
- `POST /api/ai/suggest-links` - ??????

### ???????
- `GET /api/pipeline/status` - ???????
- `POST /api/pipeline/run` - ?????(????)
- `POST /api/pipeline/analyze/{doc_id}` - ????????
- `GET /api/pipeline/structure` - ????????

### ??
- `GET /api/search?q={query}` - ??(?? keyword/semantic/hybrid)
- `GET /api/search/recommendations/{type}/{id}` - ????
- `POST /api/search/rebuild-index` - ??????

---

## ?????? LangChain

### ????:???? LLM API

???? AI ????????? `httpx` ?? OpenAI / Anthropic ? REST API,????(?????????)????????? Python ??

### ??

| ?? | ?? |
|------|------|
| **????** | ????????,prompt ???API ???????????,??????? |
| **?????** | ????? LangChain ? Chain/Agent/Tool/Retriever ?????,????????? |
| **????** | ??? `httpx` ? HTTP ??,????? LangChain ???????? |
| **???? prompt** | prompt ???????,?????????,???????????? |
| **??????** | ?? try/except HTTP ??,???????????/???? |
| **?????** | ???? LangChain ????,??????? |
| **????** | ?? LangChain ??? breaking changes ??,API ???????? URL |

### ??

| ?? | ?? |
|------|------|
| **??????** | ?? LLM ????????????(????? `call_llm()` ????) |
| **??? RAG** | ???????????????????(????? `SearchService` ??) |
| **? Agent ??** | ??????????? JSON ???,?? LangChain ? ReAct/Plan-and-Execute ??? |
| **?????** | ?????????????,??? SSE ???? |
| **? Memory ??** | ??????????,??????????? |
| **?????** | ?????? LangChain ??????(??????????) |
| **???????** | ???? AI ?? JSON ?????,???? OpenAI Function Calling ?????? |

### ??

???????**?????????????????? prompt** ??????,???? API ??????? LangChain ????LangChain ??????????????????? LLM ????????? Agent ??????

---

## ???? LangChain ??

???????? LangChain,?????????????:

### 1. AIService ?? ? LangChain Chat Models

**????:**
```python
# ai_service.py - ???? httpx
async def _call_openai(self, messages, model, temperature):
    async with httpx.AsyncClient() as client:
        response = await client.post(url, headers=headers, json=data)
    return response.json()
```

**???:**
```python
from langchain_openai import ChatOpenAI
from langchain_anthropic import ChatAnthropic

def get_llm():
    if settings.llm_provider == "anthropic":
        return ChatAnthropic(model=settings.anthropic_chat_model, ...)
    return ChatOpenAI(model=settings.openai_chat_model, ...)
```

**????:** `ai_service.py` ????,?? `_call_openai`?`_call_anthropic`?`call_llm`

### 2. ?????? ? LangChain Tools

**????:**
```python
# ai_service.py - process_with_tools() ????JSON
json_match = re.search(r'\{.*"actions".*\}', content, re.DOTALL)
parsed = json.loads(json_match.group())
for action in parsed.get("actions", []):
    method = getattr(self.file_tools, action["tool"])
    result = await method(**action["params"])
```

**???:**
```python
from langchain_core.tools import tool

@tool
async def read_raw_document(doc_id: str) -> str:
    """????????"""
    return await file_tools.read_raw_document(doc_id)

@tool
async def write_wiki_page(page_id: str, content: str, metadata: dict) -> str:
    """??Wiki??"""
    return await file_tools.write_wiki_page(page_id, content, metadata)

# ?? OpenAI ?? Function Calling
llm_with_tools = llm.bind_tools([read_raw_document, write_wiki_page, ...])
```

**????:** `tools/file_tools.py`?`tools/code_tools.py` ?? `@tool` ???,`ai_service.py` ? `process_with_tools()` ??

### 3. RAG ???? ? LangChain Retrievers

**????:**
```python
# search_service.py - ???????+??????
results = await self._keyword_search(query, doc_type)
results.extend(await self._semantic_search(query, doc_type))
```

**???:**
```python
from langchain_community.retrievers import BM25Retriever
from langchain_community.vectorstores import FAISS

# ??????
bm25_retriever = BM25Retriever.from_documents(documents, k=5)

# ?????
vectorstore = FAISS.from_documents(documents, embeddings)
vector_retriever = vectorstore.as_retriever(search_kwargs={"k": 5})

# ????
from langchain.retrievers import EnsembleRetriever
ensemble = EnsembleRetriever(
    retrievers=[bm25_retriever, vector_retriever],
    weights=[0.4, 0.6]
)
```

**????:** `search_service.py` ????,???????(FAISS/Chroma),??????????

### 4. ????????? ? LangChain Chains / LCEL

**????:**
```python
# pipeline_service.py - ??????AI????
analysis = await self._analyze_new_document(new_doc, existing_wiki)
if action == "create_new":
    result = await self._create_new_wiki(new_doc, analysis)
elif action == "merge_into":
    result = await self._merge_into_existing(new_doc, analysis)
```

**???:**
```python
from langchain_core.runnables import RunnablePassthrough, RunnableLambda
from langchain_core.output_parsers import JsonOutputParser

# ? LCEL ?????
analysis_chain = (
    {"new_doc": RunnablePassthrough(), "existing_wiki": get_wiki_summary}
    | prompt
    | llm
    | JsonOutputParser()
)

create_chain = analysis_chain | RunnableLambda(handle_create)
merge_chain = analysis_chain | RunnableLambda(handle_merge)

# ??
pipeline = RunnableBranch(
    (lambda x: x["action"] == "create_new", create_chain),
    (lambda x: x["action"] == "merge_into", merge_chain),
    RunnableLambda(handle_skip)
)
```

**????:** `pipeline_service.py` ????,?????????

### 5. ???????

```
# requirements.txt ??
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-community>=0.3.0
faiss-cpu>=1.7.0          # ? chromadb
rank-bm25>=0.2.2          # BM25?????
```

### 6. ??????

| ?? | ???? | ?? |
|------|---------|------|
| `app/services/ai_service.py` | **??** | ??? LangChain Chat Models + Tools |
| `app/services/search_service.py` | **??** | ??? LangChain Retrievers + VectorStore |
| `app/services/pipeline_service.py` | **????** | ?? LCEL ????? |
| `app/tools/file_tools.py` | **????** | ?? `@tool` ????? |
| `app/tools/code_tools.py` | **????** | ?? `@tool` ????? |
| `app/config.py` | **????** | ?? LangChain ???? |
| `requirements.txt` | **??** | ?? LangChain ???? |
| `app/models/` | **??** | ???????? |
| `app/api/` | **??** | API ?????(??????) |
| `app/services/document_service.py` | **??** | ???????? |
| `app/services/wiki_service.py` | **??** | Wiki ?????? |

### 7. ????

- **?????**:??? `AIService` ? LLM ??(?????????),???????????
- **??????**:API ?? Service ????????????,??????
- **???????**:LangChain ?????????,?????????????? bug

---

## ????

??????? [../frontend/README.md](../frontend/README.md)?

### ????:?????

**?????????,???????????**

```
????:
llm-wiki-service-demo/        ? ???(Git??)
??? backend/                   ? ??(????)
?   ??? app/
?   ??? requirements.txt
?   ??? ...
??? frontend/                  ? ??(????)
    ??? src/
    ??? package.json
    ??? ...
```

**??:**

| ?? | ?? | ??(??) |
|------|------|-------------|
| **??** | Python??????????,?? | ???????,??Nginx???? |
| **????** | ?????????????? | ??????,???? |
| **???** | Python?????,???? | ???Node??,???Python?? |
| **???** | ??????????? | ??????(???CDN??????) |
| **????** | ???????????? | ???????,???? |
| **??** | ???Python?????Node?? | ??CI/CD |

### ??????

| ?? | ?? |
|------|------|
| ?? | React 18 + TypeScript |
| ?? | Vite |
| ?? | Tailwind CSS |
| Markdown?? | react-markdown + remark-gfm + rehype-highlight |
| Markdown?? | Milkdown ? @uiw/react-md-editor |
| ?? | react-force-graph-2d |
| ???? | Zustand |
| HTTP | ky ? fetch?? |

### ????

```
??? ??      ????:Wiki??????????????
??? ???    ????(???+Markdown??)+ ????(????)
??? ??      ???? + ??pipeline + ??????
??? ??      ???? ? AI?? + ????
??? ??      ????(??)
```

### ?????

1. **P0** ???? + ???????(??? + Markdown??)
2. **P1** ???? + ????
3. **P2** ???? + ???????
4. **P3** ???? + Markdown???
